{
  "name": "@texglot/zotero",
  "version": "1.0.0",
  "description": "Zotero integration for the local TeXGlot translation service",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node scripts/test.mjs",
    "typecheck": "tsc --noEmit",
    "smoke": "node scripts/smoke.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.9.0"
  }
}
