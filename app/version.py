"""Version identifiers shared by the service and its integrations."""

# Keep this in one place so local clients can negotiate the service contract
# without duplicating a version literal in every entry point.
CORE_VERSION = "1.2.0"
